version: "0.1"

app:
  host: "0.0.0.0:50051"

data:
  index_type: string
  min_key_len: 3

indexer:
  source: postgres
  url: localhost
  user: root
  password: 1324
  content:
    table: books
    index: id
    key_columns:
      - "title"
      - "description"

